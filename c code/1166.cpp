#include<bits/stdc++.h>
using namespace std;
const int maxn=10010;
int v[maxn];
int dp[maxn];
int main()
{
	int n,s;
	while(cin>>n>>s)
	{
		memset(v,0,sizeof v);
		memset(c,0,sizeof c);
		int sum=0;
		int maxn=INT_MIN;
		int minn=INT_MAX;
		for(int i=0;i<n;i++)
		{
			cin>>v[i]>>g[i];
		}
		if(sum<s)
		{
			cout<<-1<<" "<<-1<<endl;
			continue;
		}
		for(int i=0;i<n;i++)
		{
			
		}
	}
	return 0;
} 
